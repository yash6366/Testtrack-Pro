# Procfile for Heroku / Procfile-based deployments
# Note: This is for reference. For Railway, use railway.json instead.
# For Vercel frontend, deploy separately and use Nginx reverse proxy.

web: pnpm --filter api start
